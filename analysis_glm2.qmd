---
title: "Régression logistique (b)"
---

```{=html}
<style>
img.wrapfig {
float: right; 
margin: 10px;
}

</style>
```
```{r setup, include = FALSE}
knitr::opts_chunk$set(
    collapse = TRUE,
    comment = "#>",
    cache = TRUE,
    fig.retina=TRUE,
    # fig.width=7,    # Default width
    # fig.height=5,   # Default height
    fig.dpi=800     # High resolution
)


```

## Révision

Voici la figure et le modèle du questionnaire 2 (déjà examinés). On avait vu que la précision entre les participants qui parlent d'autres langues et ceux qui ne les parlent pas *semble* être différente dans la figure. Autrement dit, les erreurs standard ne sont pas superposées.

```{r}
#| message: false
#| echo: true
#| fig-align: center
#| fig-width: 6
#| out-width: 80%
#| fig-height: 4
#| fig-dpi: 500
#| warning: false
#| fig-cap: "L'influence de parler une autre langue sur la précision d'un questionnaire hypothétique (l'axe y représente la précision moyenne, pas la variable de réponse brute, qui est originalement binaire)"
#| label: fig-q2

# On utilise ici le script déjà créé pour
# nettoyer les données :
source("Scripts/script_11_nettoyage.R")

ggplot(data = long, aes(x = langues, y = correct)) + 
    stat_summary(geom = "bar", fill = 0.2, 
                 width = 0.5, color = "black") +
    stat_summary() +
    theme_classic(base_size = 13) +
    labs(y = "Correct",
         x = "Parlez-vous d'autres langues?")
```

Notre modèle, par contre, n'est pas capable de rejeter l'hypothèse nulle par rapport à la variable `langues` ($p = 0,082$). Par conséquent, on ne peut pas conclure que le fait de parler une autre langue améliore les notes des participants dans le questionnaire en question.

```{r}
mod2 = glm(correct ~ langues, data = long, family = "binomial")

library(sjPlot)
tab_model(mod2, transform = NULL,
          string.pred = "Variables", 
          string.ci = "IC 95 %",
          string.est = "Coefficients",
          string.p = "Valeur p")
```

**N'oubliez pas que :**

-   la fonction `glm()` doit contenir l'argument `family = "binomial"`;
-   les coefficients sont toujours exprimés en *log-odds* (logs de cotes) quand on visualise le modèle avec `summary()`;
-   on peut utiliser `exp()` pour extraire les cotes des *log-odds*;
-   on évite l'utilisation des probabilités pour les effets d'un coefficient individuel, car la courbe de probabilité n'est pas constante;
-   la fonction `predict()` est extrêmement utile pour prévoir des réponses à partir des nouvelles données en utilisant un modèle (soit la régression linéaire *ou* logistique).

::: {.callout-note title="Tableau de conversion" collapse="true"}
Voici un tableau adapté à partir de @garcia_2021_dvaslr (p. 146) pour faciliter la conversion entre les unités discutées en classe. L'hypothèse nulle = $\hat\beta = 0$, ce qui équivaut intuitivement à une probabilité de 50 %.

|    P | **Odds** | **ln(odds)** = $\hat\beta$ |
|-----:|---------:|---------------------------:|
| 0.10 |     0.11 |                     --2.20 |
| 0.20 |     0.25 |                     --1.39 |
| 0.30 |     0.43 |                     --0.85 |
| 0.40 |     0.67 |                     --0.41 |
| 0.50 |     1.00 |                       0.00 |
| 0.60 |     1.50 |                       0.41 |
| 0.70 |     2.33 |                       0.85 |
| 0.80 |     4.00 |                       1.39 |
| 0.90 |     9.00 |                       2.20 |
:::

## {{< fa bullseye size=xs >}} Pratique {-}

{{< fa question-circle size=xs >}} **Question 1.** *Le temps de réaction*. Chargez `rClauseData.csv` (propositions relatives en anglais) et répondez aux questions suivantes :

a.  Enlevez les éléments de remplissage (*fillers*) et changez les classes des variables selon le besoin.

<!-- ```{r} -->
<!-- #| message: false -->
<!-- library(tidyverse) -->

<!-- rc = read_csv("donnees/base/rClauseData.csv") -->
<!-- rc = rc |>  -->
<!--     filter(Type != "Filler") |>  -->
<!--     mutate(across(where(is_character), as_factor)) -->

<!-- rc |>  -->
<!--     sample_n(size = 5) |>  -->
<!--     glimpse(width = 50) -->


<!-- ``` -->

b.  Examinez les **temps de réaction** entre les deux groupes linguistiques (figure)

<!-- ```{r} -->
<!-- #| message: false -->
<!-- #| echo: true -->
<!-- #| fig-align: center -->
<!-- #| fig-width: 6 -->
<!-- #| out-width: 80% -->
<!-- #| fig-height: 4 -->
<!-- #| fig-dpi: 500 -->
<!-- #| warning: false -->
<!-- #| fig-cap: "Temps de réaction en fonction de la langue maternelle des participants" -->
<!-- #| label: fig-rc -->

<!-- ggplot(data = rc, aes(x = L1, y = RT)) +  -->
<!--     stat_summary() + -->
<!--     stat_summary(geom = "bar", width = 0.5, -->
<!--                  alpha = 0, color = "black") + -->
<!--     theme_classic(base_size = 13) + -->
<!--     scale_x_discrete(labels = c("English" = "Anglais", -->
<!--                                 "Spanish" = "Espagnol")) + -->
<!--     labs(x = "Langue maternelle", -->
<!--          y = "Temps de réaction (log s)") -->

<!-- # On voit ici que la différence est immense entre les deux groupes. -->
<!-- ``` -->

c.  Explorez et rapportez deux modèles : une régression linéaire et une régression logistique

<!-- *Exécutons nos modèles.* -->

<!-- ```{r} -->
<!-- #| message: false -->

<!-- library(sjPlot) -->

<!-- # Régression linéaire : RT ~ L1 -->
<!-- fit1 = lm(RT ~ L1, data = rc) -->

<!-- # Régression logistique : L1 ~ RT -->
<!-- fit2 = glm(L1 ~ RT, data = rc, family = "binomial") -->

<!-- ``` -->

<!-- On commence par la régression linéaire, dont le tableau est affiché ci-dessous. On constate un effet significatif de la langue maternelle, ce qui n'est pas surprenant après avoir visualisé la @fig-rc. Comme d'habitude, l'intercept ($\hat\beta_0$) nous donne le temps de réaction moyen pour le groupe de contrôle (les anglophones) : $\hat\beta_0 = 4,29$. L'autre groupe (hispanophones) est plus lent ($\hat\beta = 4,72$, IC 95 % = \[4,46; 4,98\]), ce qui n'est pas surprenant non plus. Finalement, le coefficient de détermination est considérable : presque 60 % de la variation de temps de réaction est expliquée par la variable `L1`. -->

<!-- ```{r} -->
<!-- #| label: tbl-lm1 -->
<!-- #| tbl-cap: "Régression linéaire : `RT ~ L1`" -->
<!-- tab_model(fit1, -->
<!--           string.pred = "Variables",  -->
<!--           string.ci = "IC 95 %", -->
<!--           string.est = "Coefficients", -->
<!--           string.p = "Valeur p") -->
<!-- ``` -->

<!-- Ensuite, on examine la régression logistique pour investiguer la relation contraire, c'est-à-dire `L1 ~ RT`. On observe ici que le temps de réaction a un effet statistiquement significatif sur la variable `L1`. Autrement dit, on peut prévoir la probabilité qu'un participant soit anglophone (ou hispanophone) étant donné son temps de réaction moyen : $\hat\beta = 1,38$, IC 95 % = \[1,20; 1,59\]. Le coefficient (*slope*) étant positif, on sait que plus le temps de réaction est élevé, plus il est probable que le participant soit hispanophone. Mais comment savoir quel est le niveau de référence de la variable de réponse? Ici, les niveaux sont `English` et `Spanish`. R « traduit » ces niveaux vers `0` et `1`. Effectivement, le modèle nous donne la probabilité qu'un participant soit hispanophone étant données les variables prédictives (juste `RT` ici). Voilà pourquoi l'effet de `RT` est positif : on calcule la probabilité de `Spanish`, pas d'`English`. -->

<!-- ```{r} -->
<!-- #| label: tbl-glm1 -->
<!-- #| tbl-cap: "Régression logistique : `L1 ~ RT`" -->
<!-- tab_model(fit2, transform = NULL, -->
<!--           string.pred = "Variables",  -->
<!--           string.ci = "IC 95 %", -->
<!--           string.est = "Coefficients", -->
<!--           string.p = "Valeur p") -->

<!-- ``` -->

<!-- Pour être explicite, on pourrait créer une nouvelle colonne binaire pour notre réponse. Imaginez que je voudrais un modèle qui nous donne la probabilité d'être dans le groupe `English`. Le @tbl-glm1 et le @tbl-glm2 montrent exactement les effets inverses : alors que le @tbl-glm1 utilise la variable originale (où `Spanish` représente `1` et `English` représente `0`), le @tbl-glm2 utilise l'anglais comme variable de référence pour le `1` dans une nouvelle colonne : `English`. C'est toujours une bonne idée d'être explicite et de créer une colonne binaire pour la variable de réponse (si les niveaux de la variable ne sont pas encore `0` ou `1`). -->

<!-- ```{r} -->
<!-- #| label: tbl-glm2 -->
<!-- #| tbl-cap: "Régression logistique : `L1 ~ RT`" -->
<!-- rc = rc |>  -->
<!--     mutate(English = if_else(L1 == "English", 1, 0)) -->

<!-- # Régression logistique : L1 ~ RT -->
<!-- fit2 = glm(English ~ RT, data = rc, family = "binomial") -->

<!-- tab_model(fit2, transform = NULL, -->
<!--           string.pred = "Variables",  -->
<!--           string.ci = "IC 95 %", -->
<!--           string.est = "Coefficients", -->
<!--           string.p = "Valeur p") -->
<!-- ``` -->

<!-- ::: {.callout-tip title="Une figure des prévisions" collapse="true"} -->
<!-- Bien que le rapport des résultats d'un modèle logistique ne soit pas difficile, l'interprétation des effets peut vous poser des défis. Donc, c'est une bonne idée d'utiliser le modèle pour visualiser des prévisions à partir des nouvelles données. On a vu ce type de figure en classe. Voici les étapes nécessaires et le code : -->

<!-- 1.  Après avoir créé le modèle, utilisez la fonction `predict(..., type = "response")` pour générer des probabilités à partir des nouvelles données. -->
<!-- 2.  Créez un tableau avec les valeurs de la variable prédictive (ou des variables prédictives) et les probabilités prévues. -->
<!-- 3.  Utilisez `ggplot()` pour générar la figure. -->

<!-- ```{r} -->
<!-- #| message: false -->
<!-- #| echo: true -->
<!-- #| fig-align: center -->
<!-- #| fig-width: 6 -->
<!-- #| out-width: 80% -->
<!-- #| fig-height: 4 -->
<!-- #| fig-dpi: 500 -->
<!-- #| warning: false -->
<!-- #| fig-cap: "Prévisions à partir d'un modèle logistique" -->
<!-- #| label: fig-prev -->
<!-- # Créons une nouvelle colonne (réponses) -->

<!-- # Étape 1 : des nouvelles données -->
<!-- RT = seq(from = 3, -->
<!--          to = 9, -->
<!--          by = 0.2) -->

<!-- # Étape 2 : les prévisions -->
<!-- prev = predict(fit2, newdata = tibble(RT = RT), -->
<!--                type = "response") -->

<!-- # Étape 3 : le tableau qui combine les deux -->
<!-- tab = tibble(RT = RT, -->
<!--              P = prev) -->

<!-- # La figure : -->
<!-- ggplot(data = tab, aes(x = RT, y = P)) +  -->
<!--   geom_point(size = 3, shape = 21, color = "black") +  -->
<!--   theme_classic(base_size = 13) + -->
<!--   labs(x = "Temps de réaction", -->
<!--        y = "P(English = 1)") -->

<!-- ``` -->

<!-- Il faut noter que : -->

<!-- -   les nouvelles données utilisent un intervalle réaliste de valeurs : de 3 à 9, étant donné que l'échelle des temps de réaction est en log -->
<!-- -   la fonction `predict()` exige **toutes les variables prédictives** dans le modèle -->

<!-- Finalement, on pourrait créer une version intéractive de la figure avec l'extension `plotly`, [la meilleure extension](https://plotly.com/r/){target="_blank"} pour produire ce type de figure : -->

<!-- ```{r} -->
<!-- #| message: false -->
<!-- #| echo: false -->
<!-- #| fig-align: center -->
<!-- #| fig-cap: "Prévisions à partir d'un modèle logistique : graphique intéractif" -->
<!-- #| label: fig-prev-int -->
<!-- library(plotly) -->
<!-- fig = ggplot(data = tab |> mutate(P = P |> round(2)), aes(x = RT, y = P)) +  -->
<!--   geom_point(size = 3, shape = 21, color = "darkviolet") +  -->
<!--   theme_classic(base_size = 13) + -->
<!--   labs(x = "Temps de réaction", -->
<!--        y = "P(English = 1)")  -->

<!-- ggplotly(fig) |>  -->
<!--     config(displayModeBar = F) -->
<!-- ``` -->
<!-- ::: -->

------------------------------------------------------------------------



{{< fa question-circle size=xs >}} **Question 2.** *Le rôle de la pause*. Maintenant, on va examiner le rôle de la prosodie (la présence d'une pause `#`) dans l'interprétation des pronoms rélatifs :

-   Mary saw the daughter of the nurse <span style="color:red">*who*</span>
 likes to dance (`NoBreak`)
-   Mary saw the daughter `#` of the nurse <span style="color:red">*who*</span> likes to dance (`High`)
-   Mary saw the daughter of the nurse `#` <span style="color:red">*who*</span> likes to dance (`Low`)

Lisez les trois phrases ci-dessus et considérez qui est la personne représentée par <span style="color:red">*who*</span>. La proposition relative se réfère à quelle phrase nominale (PN) :

- the daughter (*high attachment*)
- the nurse (*low attachment*)

L'interprétation dépend de quelle langue on examine. En anglais, *low attachment* est typiquement l'option préférée (donc, *the nurse* ici). En espagnol, par contre, l'option typique est *high attachment* (donc, *the daughter* ici). La question à examiner ici est si la présence d'une pause peut changer la préférence défaut d'un participant dont la langue maternelle est l'anglais ou l'espagnol.

a.  Examinez les réponses des deux groupes en fonction des conditions considérées dans les données. Créez un graphique.

<!-- ```{r} -->
<!-- #| message: false -->
<!-- #| echo: true -->
<!-- #| fig-align: center -->
<!-- #| fig-width: 6 -->
<!-- #| out-width: 80% -->
<!-- #| fig-height: 4 -->
<!-- #| fig-dpi: 500 -->
<!-- #| warning: false -->
<!-- #| fig-cap: "Réponse en fonction de la condition et de la langue maternelle" -->
<!-- #| label: fig-rc2 -->
<!-- # Créons une nouvelle colonne (réponses) -->
<!-- rc = rc |>  -->
<!--     mutate(High = if_else(Response == "High", 1, 0)) -->

<!-- ggplot(data = rc, aes(x = Condition, y = High, color = L1)) +  -->
<!--     stat_summary(aes(fill = L1), position = position_dodge(width = 0.8), -->
<!--                  geom = "bar", width = 0.75, show.legend = FALSE, -->
<!--                  alpha = 0.3, color = "black") + -->
<!--     stat_summary(position = position_dodge(width = 0.8)) + -->
<!--     theme_classic(base_size = 13) + -->
<!--     theme(legend.position = "top") +  -->
<!--     scale_color_manual(values = c("darkviolet", "darkorange2")) + -->
<!--     scale_fill_manual(values = c("darkviolet", "darkorange2")) + -->

<!--     # scale_x_discrete(labels = c("English" = "Anglais", -->
<!--     #                             "Spanish" = "Espagnol")) + -->
<!--     labs(x = "Condition", -->
<!--          y = "Préférence moyenne pour « High »", -->
<!--          color = "Langue :") -->
<!-- ``` -->

b.  Rapportez les résultats d'une régression logistique

<!-- ```{r} -->
<!-- #| label: tbl-glm3 -->
<!-- #| tbl-cap: "Régression logistique multiple : `High ~ Condition + L1`" -->

<!-- # Régression logistique : High ~ Condition + L1 -->
<!-- fit3 = glm(High ~ Condition + L1, data = rc, family = "binomial") -->

<!-- tab_model(fit3, transform = NULL, -->
<!--           string.pred = "Variables",  -->
<!--           string.ci = "IC 95 %", -->
<!--           string.est = "Coefficients", -->
<!--           string.p = "Valeur p") -->

<!-- ``` -->

<!-- > On voit que les deux variables (`Condition` et `L1`) ont un effet significatif sur les réponses des participants. La localisation d'une pause affecte clairement leurs choix. Dans la condition `NoBreak`, où il n'y a pas de pause, les participants choisissent la phrase nominale 2 (c'est-à-dire `Low`; notre intercept) : $\hat\beta_0 = -0,53$, IC 95 % = \[-0,85; -0,22\]. Par contre, quand on ajoute une pause après la phrase nominale 1 (`High`), la probabilité d'une préférence pour `High` augmente significativement : $\hat\beta = 0,74$, IC 95 % = \[0,38; 1,11\]. En revanche, une pause après la phrase nominale 2 (`Low`) à l'effet contraire : $\hat\beta = -1,00$, IC 95 % = \[-1,36; 0,66\]. Finalement, l'effet de `L1` est également significatif ($\hat\beta = 1,4$, IC 95 % = \[1,09; 1,72\]), ce qui confirme qu'il est plus probable que les participants dont la langue maternelle est l'espagnol choisissent la phrase nominale 1 (`High`), d'où l'effet positif. -->

------------------------------------------------------------------------

<!-- <br> -->

{{< fa question-circle size=xs >}} **Question 3 (difficile).** *La rétroaction*. Maintenant, on va (ré)examiner le fichier `feedbackData.csv` :

a.  Chargez le fichier `feedbackData.csv`

<!-- ```{r} -->
<!-- #| message: false -->
<!-- #| echo: false -->
<!-- fb = read_csv("donnees/base/feedbackData.csv") -->
<!-- ``` -->

b.  Est-ce qu'on peut prévoir la rétroaction à partir de la note dans la tâche A? Si oui, quelle est la probabilité qu'un participant soit dans le groupe `Recast` si sa note est de 90?

<!-- ```{r} -->
<!-- #| message: false -->
<!-- #| echo: false -->
<!-- #| eval: false -->
<!-- #| fig-align: center -->
<!-- #| fig-width: 6 -->
<!-- #| out-width: 80% -->
<!-- #| fig-height: 4 -->
<!-- #| fig-dpi: 500 -->
<!-- #| warning: false -->
<!-- #| fig-cap: "La note peut-elle prévoir le groupe de rétroaction?" -->
<!-- #| label: fig-fb -->
<!-- # Il faut nettoyer le tableau avant de -->
<!-- # répondre à cette question -->

<!-- fb = fb |> -->
<!--   dplyr::select(-c(task_B1:task_B5)) |> -->
<!--   pivot_longer(names_to = "Tache", -->
<!--                values_to = "Note", -->
<!--                cols = task_A1:task_A5) |> -->
<!--   mutate(Recast = if_else(Feedback == "Recast", 1, 0)) -->

<!-- # Ensuite, créons une figure : -->
<!-- ggplot(data = fb, aes(y = Note, x = Recast |> as_factor())) + -->
<!--   stat_summary() + -->
<!--   theme_minimal(base_size = 13) + -->
<!--   scale_x_discrete(labels = c("0" = "Correction explicite", -->
<!--                               "1" = "Reformulation")) + -->
<!--   labs(x = "Rétroaction") -->

<!-- # Finalement, notre modèle : -->
<!-- fit4 = glm(Recast ~ Note, data = fb, family = "binomial") -->

<!-- # Si vous voulez visualiser le tableau du modèle : -->
<!-- # tab_model(fit4, transform = NULL, -->
<!-- #           string.pred = "Variables", -->
<!-- #           string.ci = "IC 95 %", -->
<!-- #           string.est = "Coefficients", -->
<!-- #           string.p = "Valeur p") -->

<!-- # La réponse : -->
<!-- # predict(fit4, newdata = tibble(Note = 90), type = "response") # <<<<<<<<<<<<< -->
<!-- # Oui, P(recast = 1 | note = 90) = 0.62 -->
<!-- ``` -->

<!-- <br> -->

<!-- ::: {style="text-align: center;"} -->

<!-- [<i class="fa-solid fa-arrow-left"></i>](10-analysis.html) [<i class="fa-solid fa-house"></i>](materiels.html) [<i class="fa-solid fa-arrow-right"></i>](12-revision.html) -->

<!-- ::: -->

## Un aperçu de notre cours

Révisons les objectifs de LNG-1100, détaillés sur monPortail :

::: {.callout-important collapse="false"}

-   Formuler et tester des hypothèses de recherche \[en linguistique\] <!-- - proposer des questions de recherche [pertinentes à la linguistique] --> <!-- - contraster des designs expérimentaux appropriés étant donnée une hypothèse spécifique  -->
-   Se familiariser avec les éléments de base de l’analyse de données quantitatives <!-- - manipuler des données en utilisant le langage R et l’extension tidyverse --> <!-- - décrire des patrons pertinents dans des données linguistiques en utilisant des tableaux et des figures --> <!-- - appliquer des modèles de régression aux données linguistiques -->
-   Interpréter et synthétiser des résultats statistiques dans un rapport scientifique <!-- - associer des résultats statistiques aux objectifs de recherche  --> <!-- - développer des rapports de recherche en utilisant Markdown et R -->

:::


Tous nos objectifs sont concentrés dans nos questionnaires et dans les deux problèmes du cours, où vous devez compléter une analyse de données complète. Donc, pendant le cours, vous avez :

1.  importé des fichiers de données en format `csv`;
2.  nettoyé des fichiers générés à partir des sondages du *Microsoft Forms* (Questionnaire 3) et du *Google Forms* (Questionnaire 4);
3.  exploré des patrons pertinents en utilisant des tableaux résumés ainsi que des graphiques appropriés, effectuant les transformations nécessaires;[^revision_b-1]
4.  exécuté et interprété des modèles statistiques (linéaire et logistique);
5.  communiqué vos résultats dans un rapport scientifique/académique en format `PDF`, dans lequel vous avez géré vos références bibliographiques en utilisant un fichier `.bib`;
6.  développé des compétences en gestion de fichiers afin d'optimiser votre flux de travail.

[^revision_b-1]: Par exemple, une colonne `incorrect/correct` vers `0/1` pour qu'on puisse calculer la précision des participants, c'est-à-dire la proportion des réponses correctes par opposition aux réponses incorrectes.

Les étapes énumérées ci-dessus combinent l'analyse de données quantitatives, la composition de documents académiques et la gestion bibliographique --- tout cela à partir d'un seul outil : RStudio. Même si vous n'utilisez pas des données quantitatives dans un projet, vous pouvez facilement le développer en utilisant Quarto. Donc, pour vos cours futurs dans le bac, vous pouvez facilement continuer à utiliser RStudio ainsi que votre bibliothèque de références.

## Les concepts les plus importants

Voici une liste de concepts statistiques discutés en classe pendant le cours.

::: {.callout-tip title="Les concepts de base" collapse="true"}
-   **Valeur** $p$ : la probabilité de trouver des données en question étant donné l'hypothèse nulle
    -   Si l'hypothèse nulle est vraie et on la rejette $\rightarrow$ erreur de type I. Autrement dit, notre effet n'est pas réel, mais notre valeur $p$ est significatif
    -   Si l'hypothèse nulle est fausse et on ne la rejette pas $\rightarrow$ erreur de type II. Autrement dit, notre effet est réel, mais notre valeur $p$ n'est pas significatif (la [puissance statistique](https://fr.wikipedia.org/wiki/Puissance_statistique#:~:text=La%20puissance%20statistique%20d%27un,réalité%20les%20groupes%20sont%20différents){target="_blank"}.))
    -   *Le problème* : on ne sait jamais si $H_0$ est vraie
-   **Intervalle de confiance** : si on répétait la même expérience $n$ fois, la valeur réelle serait dans l'intervalle en question 95 % du temps (mais l'intervalle serait différent pour chaque échantillon!). Simplement dit, l'intervalle de confiance à 95 % vous dit que la moyenne de la population est dans cet intervalle avec une probabilité de 95 % (pas trop intuitif!)
-   **Hypothèse nulle** : notre point de départ. On n'a que deux options : rejeter l'hypothèse nulle OU ne pas rejeter l'hypothèse nulle. **On ne l'accepte jamais!** Autrement dit, on ne peut pas prouver l'absence d'un effet, logiquement.[^revision_b-2]
    -   Si on n'examine qu'un groupe, $H_0 : \mu = 0$ (la moyenne est de zéro)
    -   Si on compare les moyennes de deux groupes, *a* et *b*, $H_0 : \mu_a = \mu_b$ (il n'y a pas de différence entre les deux moyennes; autrement dit, les deux groupes/échantillons viennent de la même population)
    -   Si on examine des coefficients d'un modèle, $H_0 : \beta = 0$.
-   **Intercept** ($\beta_0$) : la valeur prévue par le modèle lorsque toutes les autres variables sont `0`
-   **Slope** ($\beta_n$) : l'effet d'une variable prédictive
-   **Log-odds** : l'unité standard pour les coefficients d'une régression logistique[^revision_b-3]
-   **Transformation *wide-to-long*** : processus fréquemment nécessaire si notre tableau n'est pas *tidy*, c'est-à-dire s'il contient plus d'une observation par ligne et/ou plus d'une colonne par variable d'intérêt
:::

[^revision_b-2]: J'ai mentionné en classe que la statistique bayésienne nous permet de confirmer un effet nul. Dans notre cours, cependant, on utilise la statistique traditionnelle (fréquentiste).

[^revision_b-3]: Notez que la fonction `sjPlot::tab_model()` affiche les *odds* par défaut; consultez les dernières pages supplémentaires.

Lorsqu'on utilise un modèle simple, on veut mesurer l'effet d'une variable prédictive sur une variable de réponse. Normalement, on examine plusieurs variables prédictives en même temps (c.-à-d. un modèle multiple).

-   **Dans une régression linéaire :**
    -   vérifiez si la variable de réponse est plus ou moins normale/gaussienne;
    -   interprétez l'intercept seulement si cette interprétation est utile dans votre analyse (normalement, elle n'est pas);
    -   les coefficients utilisent toujours les mêmes unités utilisées par les variables dans le modèle;
    -   on peut utiliser la fonction `predict()` pour prévoir des réponses à partir du modèle.
-   **Dans une régression logistique :**
    -   les coefficients sont listés en *log-odds* lorsque vous utilisez `summary()` pour imprimer les résultats;
    -   on évite une interprétation d'un coefficient individuel à partir des probabilités : on utilise *log-odds* ou *odds*;
    -   si vous voulez parler des probabilités, on peut utiliser la fonction `predict(..., type = "response")` pour prévoir des réponses à partir du modèle. C'est une bonne idée de visualiser les prévisions dans une figure ([page supplémentaire de la séance 13](analysis_d.html)).

## Problème B

Notre premier problème du cours (Problème A) a produit un document `PDF`. Vous serez donc à l'aise avec la structure nécessaire pour notre dernier problème. La révision qui suit se concentre sur les méthodes statistiques utilisées dans le cours, dont certaines seront essentielles pour vos évaluations finales.

<!-- Suivez les étapes suivantes attentivement. Cliquez sur les liens pour visualiser des captures d'écran qui démontrent visuellement chaque étape. -->

<!-- ::: {.callout-note title="Comment publier votre projet" collapse="true"} -->
<!-- 1.  Visitez [Quarto Pub](http://quartopub.com){target="_blank"} pour créer un compte gratuit. Votre adresse sera `username.quarto.pub`. Donc, choisissez un `username` approprié lorsque vous créez votre compte (j'utilise `lng1100`, mais vous pourriez utilisez votre nom ou vos initiales, par exemple) -->

<!-- 2.  Cliquez sur [*Manage Profile and Tokens*](Figures/quarto-pub/5.png){target="_blank"} et créez un [nouveau jeton](Figures/quarto-pub/5.png){target="_blank"} -->

<!-- 3.  Dans RStudio, cliquer sur l'onglet [*Terminal*](Figures/quarto-pub/7.png){target="_blank"} et exécutez la commande ci-dessous. `VOTRE_FICHIER.qmd` sera votre rapport pour le projet B. Assurez-vous que le nom est **exact** et que vous êtes dans le dossier correct (appuyez sur `Tab` pour naviguer dans vos dossiers)[^revision_b-4] : `quarto publish quarto-pub VOTRE_FICHIER.qmd` -->

<!-- 4.  Choisissez [`Use another account...`](Figures/quarto-pub/8.png){target="_blank"} -->

<!-- 5.  [Autorisez la publication](Figures/quarto-pub/9.png){target="_blank"} et assurez-vous que vous êtes connecté à votre compte Quarto Pub créé ci-dessus. [Votre navigateur vous demandera l'autorisation](Figures/quarto-pub/10.png){target="_blank"} pour connecter votre compte Quarto Pub à RStudio -->

<!-- 6.  Sélectionnez *Use another name...* pour [choisir le nom de votre publication](Figures/quarto-pub/12.png){target="_blank"}. Utilisez le modèle `nom_projet_b_année` -->

<!-- 7.  Voilà! Votre navigateur ouvrira une page avec votre publication : [copiez l'adresse](Figures/quarto-pub/13.png){target="_blank"} et collez-la sur monPortail -->

<!-- 8.  Finalement, vous pouvez définir votre projet comme « [site web par défaut](Figures/quarto-pub/13.png){target="_blank"} ». Par conséquent, vous pourrez y accéder à partir de l'adresse `https://username.quarto.pub`. C'est l'option que j'ai choisie pour la page du modèle pour le projet : `https://lng1100.quarto.pub` (mon `username` ici est `lng1100`). Sinon, vous devrez utiliser `https://username.quarto.pub/nom_projet_b_année` -->
<!-- ::: -->

<!-- [^revision_b-4]: Par exemple, si votre projet `Qmd` est dans le dossier `Projet B`, il faudra taper `quarto publish quarto-pub Projet\ B/VOTRE_FICHIER.qmd`. Autrement dit, il faut s'assurer que vous utilisez l'adresse correcte par rapport au répertoire actuel de votre Terminal. -->

------------------------------------------------------------------------

## {{< fa bullseye size=xs >}} Pratique finale {-}


{{< fa question-circle size=xs >}} **Question 1.** Dans une étude examinant les effets d'une méthode fantastique d’enseignement sur la performance des étudiants, les chercheurs ont exécuté une `M` avec `X` groupes d'apprenants. Les scores de performance des étudiants ont été analysés pour déterminer s’il y avait des différences significatives entre les groupes à la fin de l'étude. Les résultats étaient les suivants :  *F*(2, 57) = 4,32, *p* < 0,038. Répondez aux questions suivantes :

a. Combien de groupes `X` étaient considérés dans l’étude?
b. La performance des participants a été calculée à partir d’un questionnaire. Combien de questions ce questionnaire comportait-il?
c. Quelles sont l’hypothèse nulle et l’hypothèse alternative de l’étude?
d. Quelle méthode statistique `M` a été employée?
e. Pouvons-nous rejeter l’hypothèse nulle?
f. Si nous rejetons l’hypothèse nulle, pouvons-nous déterminer à partir des informations données où se situent les différences parmi les méthodes d’enseignement considérées?
g. Supposons que l'objectif de l'étude est de quantifier l'efficacité de la méthode d'enseignement mentionnée. Quel est d'après vous le principal problème de l'étude?

{{< fa question-circle size=xs >}} **Question 2.** Observez le tableau ci-dessous et rapportez les résultats de l'analyse. Combien de niveaux les facteurs A--B comportent-t-ils? Combien d'observations totales avons-nous dans l'analyse?

| Source de variation | Somme des carrés | Degrés de liberté  | Moyenne des carrés | *F* | *p* |
|---------------------|-------------------|-------------|-------|-------|----------|
| Facteur A           | 6.068             | 2           | 3.034 | 9.073 | 0.000253 |
| Facteur B           | 5.122             | 1           | 5.122 | 15.316| 0.000174 |
| Residuals           | 30.765            | 92          | 0.334 |       |          |  
<br>

{{< fa question-circle size=xs >}} **Question 3.** Observez le tableau ci-dessus encore une fois. Vous vous souvenez quelle est la relation entre les valeurs dans les colonnes *Somme des carrés*, ..., *F*? Comment pouvons-nous dériver les valeur *F* des autres colonnes?

{{< fa question-circle size=xs >}} **Question 4.** Téléchargez le fichier `phonetique.RData`. Le format `RData` offre plusieurs avantages. Importez-le en utilisant la fonction `load()`. Vérifiez les classes des variables : qu'est-ce que vous notez? Il s'agit d'une étude sur l'acquisition de contrastes vocaliques en français à partir de trois groupes de participants (trois langues maternelles). Les apprenants ont étudié le français pendant une période de six mois. Les groupes ont été séparés en deux conditions (deux méthodes d'enseignement) : la condition `traditionelle`, où les séances n'avaient pas d'information explicite sur la phonétique du français, et la condition `phonétique`, où les séances avaient un élément dédié à la phonétique du français. Avant de commencer l'expérience, tous les participants on fait un test de français qui se concentrait sur l'identification des voyelles dans la langue. Après la période de six mois, un autre test a été fait. Les scores de chaque test sont dans les données. Explorez le fichier en question et répondez aux questions suivantes.

a. Créer une figure qui résume les patrons dans les données.

b. Modélisez les données et rapportez les résultats. La méthode d'enseignement a-t-elle un effet sur l'apprentissage des contrastes vocaliques?


{{< fa question-circle size=xs >}} **Question 5.** Voici les coefficients d'un modèle statistique. Vous pouvez peut-être deviner quelles sont les données modélisées ici. Les valeurs ont été arrondies à trois chiffres (donc les valeurs *p* ne sont pas vraiment zéro). Les données décrivent les passagers à bord du Titanic. Le modèle analyse la survie de ces passagers étant données trois variables, à savoir, la classe, le sexe, et l'âge (dichotomisé : enfant vs adulte) de chaque passager. Observez le résultat ci-dessous et répondez aux questions suivantes :

```{r}
#| message: false
#| echo: false

library(tidyverse)
data(Titanic)

tit = Titanic |> 
  as_tibble() |> 
  uncount(n) |> 
  mutate(Survived = if_else(Survived == "Yes", 1, 0))

fit = glm(Survived ~ Class + Sex + Age, data = tit, family = "binomial")

summary(fit)$coefficients |> round(4)
```

a. Vous devez savoir déjà quel type de modèle on utilise ici, étant donnée l'information ci-dessus. Si nos données sont dans un objet nommé `Titanic`, quelle serait la formule pour exécuter le modèle en question? Vous pouvez télécharger les données à partir du [dépôt Git](https://github.com/guilhermegarcia/LNG1100){target="_blank"} du cours.

b. Quelle était la probabilité de survie à la catastrophe si vous étiez un homme dans la deuxième classe? Et d'une femme dans la même classe?

c. Considérant les types de passagers analysés ici, quelle était le meilleure scénario possible? Calculez la probabilité de survie pour chaque combinaison possible de passager.

```{r}
#| echo: false
#| eval: false
newdata = expand.grid(Class = c("1st", "2nd", "3rd", "Crew"),
                              Sex = rep(c("Male", "Female")),
                              Age = c("Child", "Adult")) |> 
  as_tibble()

newdata = newdata |> 
  mutate(pred = predict(fit, newdata, type = "response"))

newdata |> arrange(desc(pred))
```













